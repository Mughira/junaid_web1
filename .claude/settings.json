{
  "permissions": {
    "allow": [
      "Bash(npm install pg dotenv)",
      "Bash(docker build -t kingship-test .)",
      "Bash(docker run --rm kingship-test ls -la /app/dashboard.html)",
      "Bash(docker run --rm kingship-test sh -c \"ls -la /app/dashboard.html\")",
      "Bash(docker compose logs --tail 20)",
      "Bash(docker compose exec web sh -c \"wget -qO- http://localhost:8000/dashboard | head -5\")",
      "Bash(curl -s -o /dev/null -w \"%{http_code}\" http://localhost:8000/dashboard)",
      "Bash(curl -s http://localhost:8000/dashboard)",
      "Bash(npm install stripe nodemailer)"
    ]
  }
}
